# URL API backend (opsional – hanya diperlukan jika backend berjalan di host/port berbeda)
# Contoh untuk production: VITE_API_URL=https://api.example.com/api
# Saat development dengan Vite proxy, variabel ini tidak perlu diisi.
VITE_API_URL=
